e:
g:
step(n)
step(n)
if n == egg :
	take(n)
	jump k
endif
clone nw a
clone ne b
h:
if nw != egg ||
  ne != egg :
	jump h
endif
if nw < ne :
	take(nw)
else
	take(ne)
endif
k:
step(s)
step(s)
if s == cauldron :
	drop(s)
else
	if w != floor :
		f:
		if se != floor :
			step(e)
			jump f
		endif
	else
		i:
		if sw != floor :
			step(w)
			jump i
		endif
	endif
	drop(here)
	jump j
endif
a:
c:
if w == floor :
	step(w)
	jump c
endif
jump e
b:
d:
if e == floor :
	step(e)
	jump d
endif
jump g
j:
