import map from './map0.json'

const level = {
  mapConfig: map,
  startingCode: "step(s)\nstep(s)\n",
  startingEditorType: "graph",
  maxStep: Infinity,
  speedTarget: 3,
  lengthTarget: 3
}

export default level